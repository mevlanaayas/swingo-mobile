import 'package:flutter/material.dart';

class CreateOrdersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: SafeArea(
            child: Text('Create Orders')
        )
    );
  }
}
